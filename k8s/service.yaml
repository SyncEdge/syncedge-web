apiVersion: v1
kind: Service
metadata:
  name: syncedge-web
  labels:
    app: syncedge-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: syncedge-web
